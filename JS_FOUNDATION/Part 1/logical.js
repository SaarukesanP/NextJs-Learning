// && -> and
// || -> or
// ! -> not

let isRaining = true;
let isSunny = false;

console.log(isRaining && isSunny); // false (both conditions must be true)
console.log(isRaining || isSunny); // true (at least one condition is true)
console.log(!isRaining); // false (negation of true)
